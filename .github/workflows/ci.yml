name: ci

on:
  push:
    branches: [main]
  pull_request:

env:
  TZ: "Asia/Tokyo"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup mise
        uses: jdx/mise-action@v2
      - run: npm ci
      - name: Run tests
        run: npm test
